execution:
  planner: blink
  type: streaming
  result-mode: table
  parallelism: 1
  max-parallelism: 128
  min-idle-state-retention: 0
  max-idle-state-retention: 0

deployment:
  type: standalone
  response-timeout: 5000
  gateway-address: ""
  gateway-port: 0